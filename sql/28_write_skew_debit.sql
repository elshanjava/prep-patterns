-- ══════════════════════════════════════════════════════════════════════════
--  28. Списание под гонкой (write skew)
--  [КОНКУРЕНТНОСТЬ]
-- ══════════════════════════════════════════════════════════════════════════
--  УСЛОВИЕ
--   Защита от двойного списания.
--
--  ПОДСКАЗКА
--   Два конкурентных «прочитал баланс → списал» под READ COMMITTED дают
--   write skew. Лечим: FOR UPDATE (пессимизм, блок строки) ИЛИ
--   SERIALIZABLE/SSI (оптимизм, один tx откатится). Условие balance>=100 —
--   вторая линия.
-- ══════════════════════════════════════════════════════════════════════════


-- ── твой запрос ─────────────────────────────────────────────────────────────




-- ══════════════════════════════════════════════════════════════════════════
--  ЭТАЛОН — не открывать до своей попытки
-- ══════════════════════════════════════════════════════════════════════════
/*
BEGIN;
SELECT balance FROM accounts
WHERE id=1006 FOR UPDATE;
UPDATE accounts SET balance = balance - 100
WHERE id=1006 AND balance >= 100;
COMMIT;
*/
