-- 28. Списание под гонкой (write skew)  [КОНКУРЕНТНОСТЬ]
-- Защита от двойного списания.
--
-- ПОДСКАЗКА:
--    Два конкурентных «прочитал баланс → списал» под READ COMMITTED дают write skew. Лечим:
--    FOR UPDATE (пессимизм, блок строки) ИЛИ SERIALIZABLE/SSI (оптимизм, один tx
--    откатится). Условие balance>=100 — вторая линия.


