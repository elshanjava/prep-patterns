-- ══════════════════════════════════════════════════════════════════════════
--  3. Юзеры без единой транзакции
--  [БАЗА]
-- ══════════════════════════════════════════════════════════════════════════
--  УСЛОВИЕ
--   Ни одной транзакции ни по одному счёту.
--
--  ПОДСКАЗКА
--   LEFT JOIN…IS NULL на уровне юзера ломается при нескольких счетах (уедет
--   тот, у кого хоть один счёт пуст). Нужен коррелированный NOT EXISTS.
-- ══════════════════════════════════════════════════════════════════════════


-- ── твой запрос ─────────────────────────────────────────────────────────────




-- ══════════════════════════════════════════════════════════════════════════
--  ЭТАЛОН — не открывать до своей попытки
-- ══════════════════════════════════════════════════════════════════════════
/*
SELECT u.id, u.name
FROM users u
WHERE NOT EXISTS (
  SELECT 1 FROM accounts a
  JOIN transactions t ON t.account_id = a.id
  WHERE a.user_id = u.id);
*/
