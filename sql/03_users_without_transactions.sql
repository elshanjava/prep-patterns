-- 3. Юзеры, у которых нет ни одной транзакции ни по одному счёту.
--    Вернуть: user_id, name.
--    Три способа: NOT EXISTS, LEFT JOIN ... IS NULL, NOT IN.
--    Напиши через NOT EXISTS и будь готов сказать, чем плох NOT IN (подсказка: NULL).

