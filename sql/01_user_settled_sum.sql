-- 1. Сумма проведённых (SETTLED) транзакций по всем счетам юзера.
--    Вернуть: user_id, name, total.
--    ЛОВУШКА: юзеры без счетов и без транзакций должны попасть в результат с 0,
--    а не исчезнуть. Разница между условием в ON и в WHERE при LEFT JOIN.
--    Проверь себя: в базе есть Ghost 1-3 (без счетов) и Idle 1-2 (счёт без транзакций).

