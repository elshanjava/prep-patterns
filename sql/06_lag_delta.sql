-- ══════════════════════════════════════════════════════════════════════════
--  6. LAG: дельта с предыдущей
--  [ОКНА]
-- ══════════════════════════════════════════════════════════════════════════
--  УСЛОВИЕ
--   Разница суммы с предыдущей транзакцией счёта.
--
--  ПОДСКАЗКА
--   У первой строки LAG = NULL → разница NULL. Нужен дефолт —
--   LAG(amount,1,0).
-- ══════════════════════════════════════════════════════════════════════════


-- ── твой запрос ─────────────────────────────────────────────────────────────




-- ══════════════════════════════════════════════════════════════════════════
--  ЭТАЛОН — не открывать до своей попытки
-- ══════════════════════════════════════════════════════════════════════════
/*
SELECT id, amount,
  amount - LAG(amount) OVER (
    PARTITION BY account_id
    ORDER BY created_at, id) AS diff_prev
FROM transactions WHERE account_id=1006;
*/
