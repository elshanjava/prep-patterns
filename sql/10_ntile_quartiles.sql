-- ══════════════════════════════════════════════════════════════════════════
--  10. NTILE: квартили
--  [ОКНА]
-- ══════════════════════════════════════════════════════════════════════════
--  УСЛОВИЕ
--   Разбить юзеров на 4 корзины по обороту.
--
--  ПОДСКАЗКА
--   NTILE делит максимально ровно; при неделящемся числе строк ранние
--   корзины крупнее на 1.
-- ══════════════════════════════════════════════════════════════════════════


-- ── твой запрос ─────────────────────────────────────────────────────────────




-- ══════════════════════════════════════════════════════════════════════════
--  ЭТАЛОН — не открывать до своей попытки
-- ══════════════════════════════════════════════════════════════════════════
/*
WITH turnover AS (
  SELECT a.user_id, SUM(ABS(t.amount)) AS vol
  FROM transactions t JOIN accounts a ON a.id=t.account_id
  GROUP BY a.user_id)
SELECT user_id, vol,
  NTILE(4) OVER (ORDER BY vol DESC) AS quartile
FROM turnover;
*/
