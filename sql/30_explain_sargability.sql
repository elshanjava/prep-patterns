-- ══════════════════════════════════════════════════════════════════════════
--  30. EXPLAIN: sargability
--  [ПРОД]
-- ══════════════════════════════════════════════════════════════════════════
--  УСЛОВИЕ
--   Почему индекс не используется.
--
--  ПОДСКАЗКА
--   Функция/каст на индексируемой колонке убивает индекс (не sargable) —
--   переписывай в диапазон. Проверено на 200k строк: слева Parallel Seq
--   Scan, справа Bitmap Index Scan. Та же беда — несовпадение типов колонки
--   и литерала.
-- ══════════════════════════════════════════════════════════════════════════


-- ── твой запрос ─────────────────────────────────────────────────────────────




-- ══════════════════════════════════════════════════════════════════════════
--  ЭТАЛОН — не открывать до своей попытки
-- ══════════════════════════════════════════════════════════════════════════
/*
-- ❌ функция на колонке -> Seq Scan
EXPLAIN (ANALYZE, BUFFERS)
SELECT * FROM transactions
WHERE date(created_at) = DATE '2025-03-01';

-- ✅ sargable диапазон -> Index Scan
EXPLAIN (ANALYZE, BUFFERS)
SELECT * FROM transactions
WHERE created_at >= '2025-03-01'
  AND created_at <  '2025-03-02';
*/
