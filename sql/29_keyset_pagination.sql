-- ══════════════════════════════════════════════════════════════════════════
--  29. Keyset-пагинация
--  [ПРОД]
-- ══════════════════════════════════════════════════════════════════════════
--  УСЛОВИЕ
--   Стабильная страница без OFFSET.
--
--  ПОДСКАЗКА
--   Сравнение кортежей (created_at,id) > (…) — курсор не съедет при
--   вставках, в отличие от OFFSET (у стрима это был обход всего, Java №5).
--   Нужен составной индекс (created_at,id).
-- ══════════════════════════════════════════════════════════════════════════


-- ── твой запрос ─────────────────────────────────────────────────────────────




-- ══════════════════════════════════════════════════════════════════════════
--  ЭТАЛОН — не открывать до своей попытки
-- ══════════════════════════════════════════════════════════════════════════
/*
SELECT id, account_id, created_at
FROM transactions
WHERE (created_at, id)
    > (TIMESTAMPTZ '2025-04-03 15:10', 1004)
ORDER BY created_at, id
LIMIT 5;
*/
