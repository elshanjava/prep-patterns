-- 29. Keyset-пагинация  [ПРОД]
-- Стабильная страница без OFFSET.
--
-- ПОДСКАЗКА:
--    Сравнение кортежей (created_at,id) > (…) — курсор не съедет при вставках, в отличие от
--    OFFSET (у стрима это был обход всего, Java №5). Нужен составной индекс
--    (created_at,id).


