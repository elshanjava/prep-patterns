-- 20. Пивот через FILTER  [ОТЧЁТЫ И МНОЖЕСТВА]
-- Обороты по типам транзакций в колонки.
--
-- ПОДСКАЗКА:
--    FILTER (WHERE …) чище, чем SUM(CASE WHEN … END). Пустая ячейка = NULL — оборачивай в
--    COALESCE(…,0) при необходимости.


