-- ══════════════════════════════════════════════════════════════════════════
--  11. Moving average (ROWS vs RANGE)
--  [ОКНА]
-- ══════════════════════════════════════════════════════════════════════════
--  УСЛОВИЕ
--   Скользящее среднее по 3 транзакциям.
--
--  ПОДСКАЗКА
--   ROWS = физические строки, RANGE = логический диапазон значений ORDER BY
--   — на дубликатах ключа дают РАЗНОЕ. Дефолтный фрейм при ORDER BY без
--   явного — RANGE, частый сюрприз.
-- ══════════════════════════════════════════════════════════════════════════


-- ── твой запрос ─────────────────────────────────────────────────────────────




-- ══════════════════════════════════════════════════════════════════════════
--  ЭТАЛОН — не открывать до своей попытки
-- ══════════════════════════════════════════════════════════════════════════
/*
SELECT id, amount,
  ROUND(AVG(amount) OVER (
    PARTITION BY account_id
    ORDER BY created_at, id
    ROWS BETWEEN 2 PRECEDING
             AND CURRENT ROW),2) AS mov_avg3
FROM transactions WHERE account_id=1006;
*/
